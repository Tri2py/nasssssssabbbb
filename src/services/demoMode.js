// demoMode.js — simplified for front-end only mode
// The app always works in front-end mode with localStorage

const DEMO_MODE_KEY = 'demoMode';

export const isDemoMode = () => false;

export const setDemoModeStorage = (on) => { };

export { DEMO_MODE_KEY };
